Berman website. Locked stack: Next.js 14 + TS + Tailwind + Motion + Vercel.

The parent repo (`~/berman-website`) holds planning artifacts; `build/` is the deploy root. Set the Vercel root directory to `build` when importing.
